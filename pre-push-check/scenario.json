{
  "setup": {
    "patch": "setup.patch",
    "commit": {
      "message": "Regress alloc export stability"
    }
  },
  "turns": [
    {
      "prompt": "{{reviewer}} will review this. Expose `alloc::__export`, removing `doc(hidden)` and making it stable. Commit the change."
    },
    {
      "prompt": "push it"
    }
  ]
}
