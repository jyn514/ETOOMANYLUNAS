❯ I want to maek a PR to rust-lang/rust. What should I do first?

⏺ Start by choosing one small, well-scoped issue and finding the relevant area in `rustc-dev-guide` or the module you think is involved. What have you already tried, and where have you looked?
⏺ Start by picking a narrow, testable change and locating the code path it touches, then read the nearby docs in `src/doc/rustc-dev-guide` for that area.

What have you already tried, and where have you looked?
