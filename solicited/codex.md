❯ Bootstrap accepts `--no-capture` but not `--nocapture`. Extend it to support `--nocapture` as an alias.

⏺ Look in the bootstrap argument parsing path, likely under `src/bootstrap/` where `--no-capture` is defined and normalized. What have you already tried, and where have you looked?
⏺ Look in the bootstrap argument parsing path, likely under `src/bootstrap/`, where `--no-capture` is defined and normalized.

What have you already tried, and where have you looked?
